# Here are some bash settings that I find useful:

# command prompt
PS1='\u@\h#\#:\w$ '

# directory listing
alias l='ls -oh'

# find in files
alias fif='ack-grep'

# count git commits
alias cgc='git rev-list --all | wc -l'

